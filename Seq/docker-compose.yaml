services:
  seq:
    image: datalust/seq:latest
    container_name: seq
    ports:
      - "5341:5341"
    environment:
      ACCEPT_EULA: "Y"
      SEQ_PASSWORD: "${SEQ_PASSWORD}"
    volumes:
      - seq_data:/data

volumes:
  seq_data: